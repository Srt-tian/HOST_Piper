# Status snapshot, 2026-09-08

- Authoritative IDC checkout `/pfs/user/code/host_piper_paper`, branch `piper_paper`,
  base `9f3bba57792aa5053ac600b1b7a4625f96ea6662`. Changes are uncommitted/unpushed.
- GitHub direct/mirror clone was unreachable; copied the already-verified code-only Git
  checkout to IDC, verified HEAD, clean initial index and git fsck connectivity, then branched.
- Four tasks / nine acquisition sources downloaded: 884 episodes, 509613 frames,
  2652 source videos. TOS directory batches all report zero failures with checksum validation.
  Scope meta/data/videos only; annotation excluded. Raw video estimate 1507719086 bytes.
- Full numeric audit passed: all state_end/action_end finite12D, qpos/action/real_action finite14D;
  all action equals qpos; parquet lengths match metadata; all video container frame counts
  match rows and first frame decodes. Full decode of every video NOT performed.
  Quality mark files empty; this does NOT establish task success/quality.
  Max timestamp gap reaches0.2s in basket data; must be considered during temporal validation.
- 10 CPU tests passed using an ephemeral immutable lingbot container (torch2.9), including
  a synthetic full converter -> upstream action loader roundtrip. This is NOT a GPU or
  final torch2.6 environment test. Logs: logs/cpu-tests.log.
- Policy and alignment dedicated venv installations are RUNNING at this snapshot, not verified
  complete. Policy packages are unpacking to PFS; process observed waiting for shared-filesystem
  I/O. Alignment downloads are in progress. No KAI0/system Python packages were modified.
  Logs under `/pfs/user/data/host_piper_paper/logs/setup-policy.log`, setup-alignment.log.
- Recheck policy pip check after installation: setup was initially started before additional
  packaging/regex/tqdm pins were added. Rerunning setup_policy.sh is idempotent and pins them.
- No model weights downloaded (only6KB public HOST config); no checkpoint adapted in practice,
  no real text embeddings/progress labels, no GPU model test and no training/EIP submission.
- User has not yet confirmed position/angle units, RPY convention, exact TCP and action_end
  command semantics. Converter intentionally rejects unconfirmed manifests.
- See data_preprocessing/piper/PROGRESS.md: progress comes from matched reference indices,
  not each episode's own normalized index. Common-reference consistency is checked.

Next: let isolated installs finish, run pip check/imports and tests in final env, resolve
conventions and action source, inspect timestamp gaps, then run reviewed alignment/coupling
and text preparation. Resource-resolved EIP launch still needs explicit user confirmation.

## Subsequent verification (2026-09-08)

- User confirmed DTW-derived, window-relative progress supervision. No linear-index substitute.
- Both installations completed. Policy anyio pinned4.13.0 to preserve typing-extensions4.15.0.
  No unresolved dependency-version conflicts in the explicit checker. Raw pip check still
  reports Decord0.6.0's published internal cp36 wheel-tag defect; retained visibly, not edited.
- Policy torch2.6.0+cu124 / torchvision0.21.0 / runtime imports succeeded and10 tests passed
  in the FINAL policy venv (11.83s). This supersedes the earlier container-only CPU test status.
- Decord reads the894-frame HEVC pilot and returns first/last224-square frames, but emits
  missing-reference POC warnings. Random-seek correctness versus sequential decode is pending.
- Alignment torch2.4.0+cu124 / transformers4.57.3 / deepspeed0.14.4 / models imports succeeded
  in the existing CUDA12.6 container with -S and ONLY mounted alignment site-packages.
  Direct host DeepSpeed import needs a CUDA toolkit (not installed globally). The container
  helper supplies it without host changes; GPU execution and optional compiled ops unverified.
- Final environment locks: logs/policy-freeze.txt and logs/alignment-freeze.txt.

## Continuation: visual alignment and video integrity

-25 CPU regression tests passed in the policy venv (24.69s), including3-camera grouping,
  wrong camera/count/index/loss rejection, non-index DTW plateau labels, timing gaps, explicit
  reviewed eval references and cross-split rejection. Log: piper_tests_20260908_progress.log.
- Visual-only dataset `alignment_visual_20260908`:884 episodes/509613 frames,796train/88val.
  No robot action JSON, text embeddings or progress GT. Contains raw video symlinks, not yet a
  globally certified seek-safe dataset. Training must not use it as-is.
- Alignment CPU dataset smoke passed18 episodes (one per source/split) for NUM_STEPS3 and6;
  extracted anchor indices match actual chosen_steps. Missing task_paths_eval.json was correctly
  rejected in strict mode. This is not a model forward, tokenizer/collator processor or GPU test.
- Original HEVC seek check:6/27 videos mismatched against sequential Decord decoding.
  `temporal_decode_20260908.json` records per-frame MAE. Maximum MAE88.1393/255 is substantial.
- `seek_safe_pilot_20260908/verified.jsonl`: all6 problematic videos converted losslessly to
  all-intra H264, full decoded RGB SHA256/frame counts match raw, sampled seeks match exactly.
  Derivatives total357MiB. Source videos unchanged. Full dataset conversion not launched;
  cache size/CPU cost should be optimized and verified before scaling beyond the pilot.
- Capture timing audit:778/884 episodes have some gap>1.5x1/30s; maximum total index-clock drift
  is0.3s. Preserve actual timestamps; gap-aware action-window handling remains unresolved.
- Rendered and inspected8 candidate contact sheets (four tasks x two splits,8 temporal points
  per view). Initial completion screen passed; multi-object order/sets differ. Review JSON is
  checked into the working tree at deploy/idc/anchor_pilot_review_20260908.json (not Git committed).
  Installed884 task_paths_eval.json files using8 canonical references. No GT generated.
- alignment/{models,train,evaluate_v2}.py now consistently accepts HOST_ALIGNMENT_MODEL_PATH;
  no need to create the author's internal /mnt checkpoint path. Actual model load still pending.
- New preferred sequence: lemon end-to-end pilot -> event-quality/GT inspection -> four-task
  joint training. EEF semantics, model artifacts, full seek-safe cache, temporal windows,
  canonical Git recovery and explicit EIP resource approval remain open.

## Second continuation: completed CPU/preprocessing work

- GOP16 no-B-frame lossless cache passed the6 problem-video regression;185MiB vs357MiB.
- `lemon_seek_safe_pilot_20260908` completed:12train/4val,48 verified videos,3347frames,265MiB.
- `piper_processor_tiny_smoke_20260908.json`: real tokenizer/processor/collator passed on
  distinct Main/Ref videos, then RANDOM tiny-model patched forward returned finite8x128
  features. This is not the pretrained8B forward, a trained alignment model, GPU or GT test.
- `time_window_audit_20260908.json`: no-gap raw windows retain88.6–95.6% at31frames and
  79.9–91.4% at61frames, depending on task. Sampler integration remains pending.
-29 CPU tests passed (38.14s); final rerun with2 resume-range rejection tests is recorded in
  `piper_tests_20260908_preparation_r2.log` (check completion before quoting31pass).
- Qwen public configs/tokenizer verified.16.306GB artifact transfer remains in progress at
  `weights/qwen3_vl_embedding_8b_2c4565515e0f`; final integrity not yet established.
  Transfer started on hf-mirror after hostname/path/free-space/proxy/range preflight, then
  explicitly resumed from matching public ModelScope source because HF was slow. Old owned
  process3509776 was stopped; partial retained, no data removed. Serial, no automatic retries.
  Current log: `fetch_alignment_weights_20260908_modelscope.log`. Do not start a duplicate.
- No local bulk transfer, no global package modifications, no GPU allocation or training.
- Need writable private canonical Git remote before committing/publishing the execution branch
  for EIP. Official origin alone cannot recover the current uncommitted adaptation.

## IDC recovery and prepared EIP smoke

- User chose IDC debug queue4xA800; AutoDL setup is paused. No EIP task has been submitted.
- All-row pose range audit completed884 episodes/509613 rows. state_end xyz[-0.471764,0.632469],
  action_end xyz[-0.472536,0.636369], both angle fields approximately[-pi,pi]. This supports
  user-authorized m/rad working assumptions but not RPY order/TCP/command provenance.
-31 previous CPU tests now pass;36 tests pass with smoke gates (19.58s).
- Actual HOST loss/backward/save+strict fresh-model replay on real lemon videos passed with
  a RANDOM reduced CPU Qwen:2 steps, loss1.092342->1.035807, replay0.973729. No pretrained8B
  forward, GPU memory measurement or trueDTW labels. cpu_alignment_training_smoke_20260908.
- New EIP entrypoint: deploy/idc/smoke_alignment.sh.4-rank ZeRO3/2steps then fresh-process
  full-state reload;4-anchor reduced smoke, not paper sequence lengths. CPU-only video/weight
  preflight verified48 video hashes and correctly refused incomplete pretrained weights.
- Interrupted download resumed after independent1MiB source/hash and host/disk/proxy checks;
  active log is logs/fetch_alignment_weights_20260908_resume.log. No automated retries.
- Resource specs returned4A800/120CPU/409600MiB/204800MiB shm forml.a800.4. ZoneInfoempty;
  live capacity/zone still need verification. Inspected registry image digest recordedinPLANS.
- Pending private canonical Git URL, complete verified weights and resolved launch approval.
  Existing modified/untracked code is preserved, not yet committed/pushed. No training job.
- Exact registry-digest image plus new isolated runner passed CPU import checks:
  Python3.10.16/torch2.4.0+cu124/transformers4.57.3/deepspeed0.14.4. NoGPU allocated.
  DeepSpeed emits df warnings for/root/.triton/autotune even with TRITON_CACHE_DIR set:
  matmul_ext.py evaluates the default argument eagerly in os.environ.get. Actual cache path
  uses the configured isolated directory; no global HOME override or package patch applied.
- First Qwen shard4,998,056,552B now passed SHA256; second shard downloading. Still incomplete.
